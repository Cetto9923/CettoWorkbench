# 复制给 Gemini 的执行提示词

你是本次员工工作台四页优化的执行工程师。请在以下仓库按照已经写好的Plan实施，不能重新自由设计一套风格，也不能只改颜色后宣称完成。

仓库：/Users/yuyan9923/GitHub/workbench-claude-po
分支：Claude-PO
计划编制基线：2f2580a10f45db5978736f1fe1e46c258133022f
用户运行地址：http://127.0.0.1:8093
目标：/home、/todos、/done、/notice。

必须先完整读取：
1. AGENTS.md。
2. docs/engineering/architecture.md、database.md、frontend.md、shared-frontend.md、testing.md、quality.md。
3. docs/plan/personal-workbench-ui-20260905/PLAN.md，尤其业务边界、冻结视觉规格、修改范围、测试和Git安全。
4. 相关.cursor/rules及当前模块有效设计文档。

执行前完成Plan的Preflight。HEAD不同先读差异更新证据，禁止切回旧版本。保护全部WIP，尤其workboard.js和workboard.html；它们不是本次范围。

按UI-00 → UI-01 → UI-02 → UI-03 → UI-04 → UI-05顺序实施，每包报告验证结果。不要把“需要更多信息”当作不做已经明确工作的理由。DATA-01仅在Plan的业务排序、独立实施授权及隔离数据库前置全部满足后执行，否则单列BLOCKED，继续其余包。

冻结方向：
- 企业内部高密度工作台；统一标题、间距、概览、分类、筛选、状态和分页；不迁移框架。
- 三列表共享呈现和请求原语，保留业务分类/列/统计口径差异。
- 首页移除重复前三条卡片，突出真实行动列表，处理宽屏留白；未有真动作的指标改非交互统计，不猜筛选接口。
- 修复错误与空态混淆、请求乱序、重置不一致、通知写失败无反馈、通知文本残留；保留鉴权和CSRF。
- 待办未接入的数据源明确标未接入，不自行设计跨模块责任政策。
- 通知不从正文猜objectID，不删除历史记录，不因“已读”改变业务处理状态。
- 首页全量加载问题与UI独立验收，不声称CSS修复等于性能治理。

每次改动要能追溯到Plan条目。已有make check绿灯只是回归门禁，不是UI已验收。先复用当前共享能力；appFetch返回原始Response，不能误认为已解析JSON或自动抛HTTP错误。

严禁：修改其他页面/治理卡、重写全局Rules、放宽baseline、改权限/业务范围/状态定义、真实库写测试、擅自重启8093、自动commit/push、引入通用列表引擎。现有已完成治理结果不重做。

必须补行为测试，并在同视口查看四页，覆盖宽屏、笔记本、窄屏、加载、空、失败、重试、乱序、筛选、分页、键盘。使用当前已登录浏览器；不要在报告复制业务正文、凭据或会话数据。需要登录让用户在浏览器操作，不索取密码文本。

最后在同目录写EXECUTION-REPORT.md，列实际文件、测试、截图位置、每个工作包、剩余门、未验证事项。未获授权不commit/push。明确区分UI完成、后端性能BLOCKED、业务接入WAIT DECISION；不得用总括的“全部完成”隐藏缺口。
