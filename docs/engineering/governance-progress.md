# 治理计划执行记录

## 执行依据

- 原计划：[Astra 制定并经用户确认的计划](governance-plan.md)，从当前任务的原始 Plan 消息原文保存。
- 工程真源仍为根 `AGENTS.md`；本记录只追踪执行，不覆盖工程规则或业务需求。
- 核对日期：2026-09-05；起点分支：`Claude-PO`；起点 HEAD：`f5f97a42b802b2eadabb393f18a88bb8fcb1aeec`。
- 起点已有未跟踪内容：`.exec.log`、`docs/engineering/phase2-audit.md`。保留原样，审计稿结论须重新核验。
- 历史 Phase 1 的窄范围约束属于先前任务；本轮依照已确认的分阶段计划执行，不把历史记忆当成本轮计划。

## 状态

| 阶段 | 状态 | 本次证据及剩余验收 |
|---|---|---|
| 0 规范入口和问题清单 | 进行中 | 已恢复原计划并核对工作区；模块索引、共享能力目录、统一问题清单和五工具冷启动尚未完成 |
| 1 安全修复 | 未开始 | 按原计划同步交付反向测试、修复和接口证据 |
| 2 架构及基础债 | 未开始 | 不提前进行跨阶段重构 |
| 3 性能及前端 | 未开始 | 先核定业务契约，再获取优化前后证据 |
| 4 门禁及 GitHub 交付 | 未开始 | 本地检查不代表远端 CI，流程约定不代表远端强制保护 |
| 大模型边界规范 | 未开始 | 仅文档，不创建 AI 实现或空抽象 |

## 防止执行偏移

1. 每批修改标明对应阶段、允许修改范围和验收命令；以当前代码复核历史发现。
2. 原计划中的验收不得因时间、模型切换或上下文摘要而删减；不能运行的验收单独标记。
3. Hermes 按原计划优先读取根 `AGENTS.md` 并检查遮蔽，不默认新增 `.hermes.md`。
4. 文档存在仅证明入口已提供；五工具兼容必须有真实新会话证据。
5. 阶段 0 保持精简，完成入口和清单后推进安全修复。每阶段更新本记录，未经证据不宣称阶段完成。

## 当前批次

允许修改：原计划存档、执行记录、README 工程导航。业务代码、权限、数据库和运行配置本批无变更。

验收：原计划存档逐字比对原始 Plan；`git diff --check`；`make check`。
本批结果：原文比对一致，`git diff --check` 和 `make check` 均通过。
门禁仍接受存量债：6 个格式文件、1 条 vet 诊断、20 个超长文件、
67 条 advisory、3 个敏感值指纹、1 个分层文件；未将这些问题标为已关闭。
完整阶段验收仍待实施，不能据此宣称整体治理完成。
