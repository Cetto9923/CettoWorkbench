app:
  name: 个人工作台
  env: dev
  addr: ":8080"

database:
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "zentao"
  dbname: "workbench"
  charset: "utf8mb4"
  parseTime: true
  loc: "Local"

zentao:
  # 禅道地址
  url: "https://changshu.wrk.oop.cc/"
  # 禅道 API 地址
  api: "https://customer.chandao.net/api.php/v1"
  account: "encrypt"
  password: "Asd123456"

session:
  cookieName: workbench_session
  lifetimeHours: 2

log:
  level: debug
  dir: logs

layout:
  # 导航样式 sidebar(侧边导航) | top(顶部导航)
  nav: sidebar
