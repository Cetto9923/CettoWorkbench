# workbench · Claude Code entry

Load and follow the repository's canonical rules before editing:

@.cursorrules
@.cursor/rules/conventions.mdc
@AGENTS.md

The Cursor rule files are the canonical source. This file only makes them
discoverable to tools that do not load `.cursor/rules/` automatically.
