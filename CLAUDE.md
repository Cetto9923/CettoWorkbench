# Claude Code adapter

Read `@AGENTS.md` in full before editing. It is the only normative rule source;
this adapter defines none. Run its pre-flight, keep the diff task-scoped, and
finish with `git diff --check` + `make check`, reporting any failed/skipped gate.
