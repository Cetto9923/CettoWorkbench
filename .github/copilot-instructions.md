# Workbench engineering entry

Read root `AGENTS.md` and `docs/engineering/agent-onboarding.md` before editing,
then task-relevant engineering docs. All paths are repository-relative.
The constitution defines engineering/business truth; this adapter is only a pointer.

Report actual files read, branch/HEAD and WIP. Preserve other work. Follow
capability, shared-database, structure and regression contracts. Run
`git diff --check`, `make check` and affected tests; report failed/skipped gates.
Verify loading using `docs/engineering/agent-compatibility.md`.
